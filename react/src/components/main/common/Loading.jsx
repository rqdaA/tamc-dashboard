import React from 'react';
import './loading.scss'

function Loading() {
    return (
        <div className="loading">
            <div className="loadingSpinner"/>
        </div>
    );
}

export default Loading;